version: "3.9"

networks:
  dev:

services:
  nginx: 
    image: nginx:stable-alpine
    ports:
      - "1900:1900"
    volumes:
        - './nginx.conf:/etc/nginx/nginx.conf'
    depends_on:
      - backend
      - frontend
    networks:
      - dev

  backend: 
    build:
      context: ./backend
    networks:
      - dev

  frontend: 
    build:
      context: ./frontend
    networks:
      - dev